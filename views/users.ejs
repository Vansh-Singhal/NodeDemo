<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="container w-full h-full bg-white">
        <h1 class="py-3 mx-5 my-2 tracking-tighter text-2xl">All Users</h1>
        <div class="users mx-3 gap-3 my-5 flex flex-wrap">
            <% if (users.length === 0){ %>
                <h2 class="text-xl text-zinc-700">No users yet...</h2>
            <% }else{  %>
                <% users.forEach((user)=>{ %>
                    <div class="user p-4 bg-slate-200 w-72 rounded-lg">
                        <img class="w-full h-52  rounded-lg object-cover overflow-hidden" src="<%=user.image%>" alt="">
                        <h3 class="mt-2 text-xl tracking-tighter"><%=user.name%></h3>
                        <h4 class="text-md tracking-tighter text-zinc-700"><%=user.email%></h4>
                        <div class="buttons mt-4">
                            <a class="tracking-tighter text-zinc-500" href="/edit/<%=user._id%>">Edit User</a>
                            <a class="py-2 px-2 text-sm text-white bg-red-500 rounded-lg" href="/delete/<%=user._id%>">Delete User</a>
                        </div>
                    </div>
                <% })%>
            <% } %>
        </div>
    </div>
</body>
</html>